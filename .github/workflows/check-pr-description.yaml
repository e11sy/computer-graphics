on:
  pull_request:
    types:
      - opened
      - ready_for_review

jobs:
  check-linked-issue:
    runs-on: ubuntu-latest
    uses: Kuchizu/CodeX-Assistant/.github/workflows/check-description.yml@main
    with:
      PR_DRAFT_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      mode: comment

